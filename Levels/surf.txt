assign [_DirectionalAnimationComponent.direction 0_], |direction_perso|
assign %calculate {0} + #arg0#%, |same_value|
if [|same_value|] == 1
	assign 0, |same_value|
else
	assign 1, |same_value|
endif

if [|same_value|] == 0
	remove_component InputComponent, 0
	log InputComponent disabled
	if #arg0# == true
		log water authorized
		block_authorize 121, 0, false
	endif
	move 0, [|direction_perso|], 15, ./Levels/surf_callback.txt, #arg0#
endif

end